name: Deploy BraysTech to SmarterASP

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # Upgraded to v4 to support Node 24 and stop the warnings
      - name: Checkout code
        uses: actions/checkout@v4

      # Upgraded to v4
      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      # Target your main web project directly instead of the whole solution file
      - name: Restore dependencies
        run: dotnet restore

      # Target the specific project to prevent build output collision
      - name: Build
        run: dotnet build --configuration Release --no-restore

      # FIX: Specify your main web project name here (e.g., BraysTech.csproj)
      # If it's inside a subfolder, use the path: run: dotnet publish YourSubfolder/BraysTech.csproj ...
      - name: Publish
        run: dotnet publish BraysTech.csproj --configuration Release --no-build --output ./publish

      - name: Create app_offline.htm to stop running app
        run: |
          mkdir -p ./offline
          echo "<html><body><h2>Updating, back shortly...</h2></body></html>" > ./offline/app_offline.htm

      - name: Upload app_offline.htm
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ${{ secrets.BRAYS_FTP_SERVER }}
          username: ${{ secrets.BRAYS_FTP_USERNAME }}
          password: ${{ secrets.BRAYS_FTP_PASSWORD }}
          local-dir: ./offline/
          server-dir: ${{ secrets.BRAYS_FTP_PATH }}

      - name: Wait 15 seconds for IIS to release files
        run: sleep 15

      - name: Deploy application files
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ${{ secrets.BRAYS_FTP_SERVER }}
          username: ${{ secrets.BRAYS_FTP_USERNAME }}
          password: ${{ secrets.BRAYS_FTP_PASSWORD }}
          local-dir: ./publish/
          server-dir: ${{ secrets.BRAYS_FTP_PATH }}
          exclude: |
            **/.git*
            **/.git*/**
            **/appsettings.Development.json
            **/appsettings.json
            app_offline.htm

      - name: Clear app_offline.htm contents
        run: |
          mkdir -p ./bring-online
          touch ./bring-online/app_offline.htm

      - name: Overwrite app_offline on server
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ${{ secrets.BRAYS_FTP_SERVER }}
          username: ${{ secrets.BRAYS_FTP_USERNAME }}
          password: ${{ secrets.BRAYS_FTP_PASSWORD }}
          local-dir: ./bring-online/
          server-dir: ${{ secrets.BRAYS_FTP_PATH }}
          log-level: verbose