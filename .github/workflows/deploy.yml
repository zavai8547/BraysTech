name: Deploy BraysTech to SmarterASP

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      # Target the specific project for clean dependency tracking
      - name: Restore dependencies
        run: dotnet restore BraysTech/BraysTech.csproj

      # Target the specific project for compilation
      - name: Build
        run: dotnet build BraysTech/BraysTech.csproj --configuration Release --no-restore

      # Target the specific project for output distribution
      - name: Publish
        run: dotnet publish BraysTech/BraysTech.csproj --configuration Release --no-build --output ./publish

      - name: Create app_offline.htm to stop running app
        run: |
          mkdir -p ./offline
          echo "<html><body><h2>Updating, back shortly...</h2></body></html>" > ./offline/app_offline.htm

      - name: Upload app_offline.htm
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ${{ secrets.BRAYS_FTP_SERVER }}
          username: ${{ secrets.BRAYS_FTP_USERNAME }}
          password: ${{ secrets.BRAYS_FTP_PASSWORD }}
          local-dir: ./offline/
          server-dir: ${{ secrets.BRAYS_FTP_PATH }}

      - name: Wait 15 seconds for IIS to release files
        run: sleep 15

      - name: Deploy application files
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ${{ secrets.BRAYS_FTP_SERVER }}
          username: ${{ secrets.BRAYS_FTP_USERNAME }}
          password: ${{ secrets.BRAYS_FTP_PASSWORD }}
          local-dir: ./publish/
          server-dir: ${{ secrets.BRAYS_FTP_PATH }}
          exclude: |
            **/.git*
            **/.git*/**
            **/appsettings.Development.json
            **/appsettings.json
            app_offline.htm

      - name: Clear app_offline.htm contents
        run: |
          mkdir -p ./bring-online
          touch ./bring-online/app_offline.htm

      - name: Overwrite app_offline on server
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ${{ secrets.BRAYS_FTP_SERVER }}
          username: ${{ secrets.BRAYS_FTP_USERNAME }}
          password: ${{ secrets.BRAYS_FTP_PASSWORD }}
          local-dir: ./bring-online/
          server-dir: ${{ secrets.BRAYS_FTP_PATH }}
          log-level: verbose